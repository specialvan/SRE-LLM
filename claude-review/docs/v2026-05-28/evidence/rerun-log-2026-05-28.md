# Evidence rerun log — 2026-05-28

## 环境

- Repo: `D:\workspace\SRE-LLM\spacex`
- Branch: `spacex-session`
- Review target: codex current working tree, `docs/opus-review/OPUS_REVIEW_PACKET.md`

## 1. Full pytest

Command:

```bash
python -m pytest tests -q
```

Result:

```text
Exit code 1
...
FAILED tests/test_control_center_browser_smoke.py::test_system_browser_smoke_decodes_utf8_dom_dump
FAILED tests/test_control_center_browser_smoke.py::test_system_browser_smoke_gives_async_fetch_time
FAILED tests/test_control_center_browser_smoke.py::test_dom_assertion_ignores_error_literals_in_script_source
FAILED tests/test_control_center_browser_smoke.py::test_dom_assertion_rejects_rendered_error_message
AssertionError: browser DOM dump missing: ['safety-budget-list', 'data-budget-focus']
```

Interpretation:

- `437 passed` claim is not current.
- Current suite is blocked by browser smoke DOM contract drift.

## 2. Evidence manifest/report

Command:

```bash
python -m analysis.evidence_manifest && python -m analysis.evidence_report
```

Result:

```text
wrote D:\workspace\SRE-LLM\spacex\analysis\artifacts\event_evidence_manifest.json
evidence_scope synthetic_sre_event_evidence
s10_failure_trace section=10 events=16
s11_catch_sre_wrapper section=11 visible=1.0
s12_sre_replay section=12 events=11.0 ticks=19
artifact_check ok studies=3 files=8
```

Interpretation:

- S10/S11/S12 event evidence chain passes.
- This pass does not cover control-center browser smoke saved manifest freshness.

## 3. quality_gate_counts

Command:

```bash
python -u -m scripts.quality_gate_counts
```

Result:

```text
Exit code 1
Traceback (most recent call last):
  File "D:\workspace\SRE-LLM\spacex\scripts\quality_gate_counts.py", line 439, in <module>
    main()
  File "D:\workspace\SRE-LLM\spacex\scripts\quality_gate_counts.py", line 434, in main
    count = update_quality_gate_docs(args.repo_root)
  File "D:\workspace\SRE-LLM\spacex\scripts\quality_gate_counts.py", line 404, in update_quality_gate_docs
    current_count = collect_pytest_count(repo_root)
  File "D:\workspace\SRE-LLM\spacex\scripts\quality_gate_counts.py", line 294, in collect_pytest_count
    control_center_evidence = package_smoke.verify_control_center_evidence_report(...)
  File "D:\workspace\SRE-LLM\spacex\scripts\package_smoke.py", line 182, in verify_control_center_evidence_report
    control_center_browser_smoke.verify_evidence_manifest(...)
  File "D:\workspace\SRE-LLM\spacex\scripts\control_center_browser_smoke.py", line 429, in verify_evidence_manifest
    _assert_dumped_dom(dom, payload)
  File "D:\workspace\SRE-LLM\spacex\scripts\control_center_browser_smoke.py", line 810, in _assert_dumped_dom
    raise AssertionError(f"browser DOM dump missing: {missing}")
AssertionError: browser DOM dump missing: ['safety-budget-list', 'data-budget-focus']
```

Interpretation:

- `quality_gate_counts` is not a passing self-healing gate in the current workspace.
- Saved browser evidence is stale relative to current DOM assertion.

## 4. SignalFusion NaN reproduction

Command:

```bash
python - <<'PY'
import json, math
import numpy as np
from sre_control import SignalFusion, Signal
fusion = SignalFusion(x0=np.array([1.0]), P0=np.eye(1), Q=np.zeros((1, 1)), x_ref=np.array([1.0]), theta=0.0)
sig = Signal(name='metrics', h=lambda x: x, H=lambda x: np.eye(1), R=np.eye(1))
trace = fusion.step(1.0, [(sig, np.array([float('nan')]))])
print('state=', fusion.state.tolist())
print('covariance=', fusion.covariance.tolist())
print('trace_signals=', trace['signals'])
print('trace_events=', trace['events'])
print('json_allows_nan=', json.dumps(trace)[:200])
print('state_has_nan=', any(math.isnan(float(v)) for v in fusion.state))
PY
```

Output:

```text
state= [nan]
covariance= [[0.5]]
trace_signals= [{'signal': 'metrics', 'used': True, 'residual': nan, 'innovation_mahalanobis': 0.0, 'threshold_used': None, 'consecutive_rejections': 0}]
trace_events= []
json_allows_nan= {"x": [NaN], "P_trace": 0.5, "signals": [{"signal": "metrics", "used": true, "residual": NaN, "innovation_mahalanobis": 0.0, "threshold_used": null, "consecutive_rejections": 0}], "local_states": ["pr
state_has_nan= True
```

## 5. SLOGuardrail approve NaN reproduction

Command:

```bash
python - <<'PY'
import numpy as np
from sre_control import SLOGuardrail

guard = SLOGuardrail(nominal_direction=np.array([0.0, 0.0, 1.0]), magnitude_cap=100.0)
for proposal in ([float('nan'), 0.0, 0.0], [float('inf'), 0.0, 0.0]):
    try:
        approved = guard.approve(proposal)
        print('approve_input=', proposal)
        print('approve_output=', approved.tolist())
        print('output_finite=', bool(np.isfinite(approved).all()))
    except Exception as exc:
        print('approve_raised=', type(exc).__name__, str(exc))
    try:
        audit = guard.audit(proposal)
        print('audit_output=', audit)
    except Exception as exc:
        print('audit_raised=', type(exc).__name__, str(exc))
PY
```

Output:

```text
approve_input= [nan, 0.0, 0.0]
approve_output= [nan, nan, nan]
output_finite= False
audit_raised= AdapterInputError non-finite proposal
approve_input= [inf, 0.0, 0.0]
approve_output= [nan, nan, nan]
output_finite= False
audit_raised= AdapterInputError non-finite proposal
```

## 6. CanaryScheduler rejected warm-start reproduction

Command:

```bash
python - <<'PY'
from sre_control import CanaryScheduler
s = CanaryScheduler(slo_error_budget=0.01, eta_init=0.1, eta_min=0.005, eta_max=0.3)
for idx in range(1, 4):
    step = s.observe(current_share=0.5, proposed_share=0.6, observed_error_rate=0.03)
    print('step', idx, 'eta=', s._eta, 'trust_region=', step.trust_region, 'accepted=', step.accepted, 'states=', step.local_states)
    for event in step.events:
        print('  event.safe_action=', event['safe_action'], 'event.trust_region=', event.get('trust_region'))
PY
```

Output:

```text
step 1 eta= 0.1 trust_region= 0.1 accepted= False states= ['observe', 'initialise', 'freeze']
  event.safe_action= shrink trust region and freeze rollout progress event.trust_region= 0.1
step 2 eta= 0.15000000000000002 trust_region= 0.15000000000000002 accepted= False states= ['observe', 'expand', 'freeze', 'refit_rejected']
  event.safe_action= shrink trust region and freeze rollout progress event.trust_region= 0.15000000000000002
step 3 eta= 0.22500000000000003 trust_region= 0.22500000000000003 accepted= False states= ['observe', 'expand', 'freeze', 'refit_rejected']
  event.safe_action= shrink trust region and freeze rollout progress event.trust_region= 0.22500000000000003
```

## 7. run_all artifacts_dir reproduction

Command:

```bash
python - <<'PY'
import tempfile
from pathlib import Path
from analysis import run_all
out = Path(tempfile.mkdtemp())
run_all.main(studies=['analysis.s10_failure_trace'], artifacts_dir=out)
print('custom_summary_exists=', (out / 'SUMMARY.txt').exists())
print('custom_trace_exists=', (out / 's10_trace_full.jsonl').exists())
print('default_trace_exists=', Path('analysis/artifacts/s10_trace_full.jsonl').exists())
print('custom_dir_files=', sorted(p.name for p in out.iterdir()))
PY
```

Output:

```text
custom_summary_exists= True
custom_trace_exists= False
default_trace_exists= True
custom_dir_files= ['SUMMARY.txt']
```
