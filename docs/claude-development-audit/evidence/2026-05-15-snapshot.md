# 2026-05-15 Evidence Snapshot

## Repository

Command:

```bash
git status --short --branch
```

Observed before writing this package:

```text
## spacex-session...origin/spacex-session
```

Command:

```bash
git log --date=iso-strict --pretty=format:"%h%x09%ad%x09%an%x09%s" -n 40
```

Observed head:

```text
13e534e  2026-05-14T21:17:52+08:00  yangqidog  chore: 同步最新分析结果 SUMMARY.txt
```

Branch status:

```text
main           cf9c8dc chore: initial backup before Claude changes
spacex-session 13e534e [origin/spacex-session] chore: 同步最新分析结果 SUMMARY.txt
```

Tracked review surface:

```text
git ls-files docs wiki starship sre_control analysis tests scripts examples -> 148 files
```

## Tests

Command:

```bash
python -m pytest --collect-only -q
```

Observed collection:

```text
tests/test_allocation.py: 1
tests/test_contracts.py: 15
tests/test_control_center.py: 4
tests/test_ekf.py: 4
tests/test_event_schema.py: 3
tests/test_failure_trace.py: 10
tests/test_import_graph.py: 2
tests/test_mpc.py: 1
tests/test_quaternion.py: 3
tests/test_rigid_body.py: 2
tests/test_sre_control.py: 21
tests/test_stability_monitor.py: 8
tests/test_thrust_constraints.py: 3
```

Total: 77 collected tests.

Command:

```bash
python -m pytest tests -q
```

Observed result:

```text
Exit code 0 over the 77 collected tests.
```

The local pytest output was dot-only due the current quiet settings, so this snapshot records the exit code plus the explicit collection count.

## Analysis Suite

Command:

```bash
python -m analysis.run_all
```

Observed result:

```text
All 10 studies finished in 2.80s. Artifacts in analysis/artifacts/.
```

Stable behavioral observations:

| Study | Key observation |
|---|---|
| §1 Lossless | `pos_err 148.3 -> 2.125e-06` |
| §5 EKF | `vel_rmse 481.1 -> 51.07`; `pos_p95 59.44 -> 77.34` remains a counterexample |
| §8 Allocation | `saturation_violation_pct 33.75 -> 0`; residual does not disappear |
| §9 SRE Stack | `slo_violation_pct 25 -> 10`; mean replicas increase |
| §10 Failure Trace | `event_count_total=14`, `distinct_kinds=4`, `background_event_fraction=0.0`, `event_visible_fraction=0.8461538461538461` |

Note: `analysis.run_all` rewrites timing fields in `analysis/artifacts/SUMMARY.txt`. Timing deltas were treated as machine noise and not retained as review evidence.

## File Evidence Anchors

| Area | Anchor |
|---|---|
| Package include surface | `pyproject.toml:20-22` |
| SRE public API | `sre_control/__init__.py:22-61` |
| README SRE stack entry | `README.md:220-239` |
| Control center entry | `README.md:186-198` |
| Allocator signature | `sre_control/stack.py:89-107` |
| Allocator matrix uses zone vectors | `sre_control/weighted_balancer.py:50-57` |
| Current adapter exception taxonomy | `sre_control/stack.py:23-36`, `docs/EVENT_SCHEMA.md:37-46` |
| Stale PR invariant | `PR-REQUIREMENTS.md:13-20` |
| Correct EKF Joseph form | `starship/ekf.py:91-97` |
| Stale Joseph snippets | `docs/SPECIAL_SOLUTIONS_DERIVATIONS.md:44-45`, `docs/SPECIAL_SOLUTIONS_DERIVATIONS.md:826` |
| HTML V1 marker | `docs/knowledge-base.html:173` |
| HTML V2 migration note | `docs/V2_Knowledge/knowledge-base.html:942` |
